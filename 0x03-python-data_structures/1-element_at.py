#!/usr/bin/python3
def element_at(my_list, idx):
    for i in range(len(my_list)):
        if index(i) < 0 or index(i) > (len(my_list -1):
            return (None)
        else:
            return(mylist(len(i)))
